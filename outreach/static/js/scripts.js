// Wait for document to be ready
$(document).ready(function() {
    // Initialize Fancybox
    if ($.fn.fancybox) {
        $('.fancybox').fancybox();
    }
    
    // Add other initializations and custom code here
});